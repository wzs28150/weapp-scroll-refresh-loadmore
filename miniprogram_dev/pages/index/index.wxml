<view class="page">
  <view class="inner">
    <view class="logo">
      <image src="../../img/logo.png" class="logo"></image>
    </view>
    <view class="list">
      <navigator wx:for="{{list}}" wx:key="title" url="{{item.path}}">{{item.title}}</navigator>
    </view>
  </view>
</view>